// examples/demo.ltl — runnable demo for ltl_snake

import std::io
use ltl_snake as lib

fn main() {
    io::println("ltl_snake demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}